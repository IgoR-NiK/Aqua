<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:XamarinFormsApp.ViewModels;assembly=XamarinFormsApp"
             x:DataType="viewModels:MainViewModel"
             x:Class="XamarinFormsApp.Views.MainView">
    <ContentPage.Content>
        <StackLayout>
            <Editor Text="{Binding Text}" />
            <Button Command="{Binding GoToSecondCommand}" Text="Goto second" />
            <Button Command="{Binding ReadConfig}" Text="Read Config" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>